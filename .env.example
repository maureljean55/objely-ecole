# Supabase — Project Settings > API (use the NEW objely-ecole project, not objely).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Server-only (never NEXT_PUBLIC_). Bypasses RLS — only for trusted server code.
SUPABASE_SECRET_KEY=
